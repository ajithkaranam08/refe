// src/routes/index.ts

import express from 'express';
// import userRoutes from './userRoutes'; // Adjust the path to your user routes

const router = express.Router();

// Use the imported routes
// router.use('/users', userRoutes);

// Export the router
export default router;
